// Setup global dos testes (carregado antes de cada arquivo de teste).
// Adiciona os matchers do jest-dom ao `expect` do Vitest, ex.:
//   expect(elemento).toBeInTheDocument()
//   expect(botao).toBeDisabled()
import '@testing-library/jest-dom/vitest'
