/**
 * Painel (esqueleto).
 * Próxima etapa: últimos registros, total do dia e do mês (R$), atualizados em
 * tempo real; depois, tabela de mais desperdiçados e ranking (só gestão).
 */
export default function Painel() {
  return (
    <section className="mx-auto max-w-3xl p-6">
      <h1 className="text-2xl font-semibold text-teal-700">Painel</h1>
      <p className="mt-2 text-gray-600">
        Em breve: totais do dia e do mês, últimos registros e relatórios.
      </p>
    </section>
  )
}
