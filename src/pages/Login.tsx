/**
 * Tela de Login (esqueleto).
 * Próxima etapa: login simples por funcionário (toca no nome + PIN de 4 dígitos).
 */
export default function Login() {
  return (
    <section className="mx-auto max-w-md p-6">
      <h1 className="text-2xl font-semibold text-teal-700">Login</h1>
      <p className="mt-2 text-gray-600">
        Em breve: selecionar o funcionário e digitar o PIN de 4 dígitos.
      </p>
    </section>
  )
}
